# Documentación para el agente

Especificaciones y notas de trabajo propio (fuera de upstream).

Deja aquí los ficheros `.md` con los cambios a implementar. El agente debe leer esta carpeta antes de tocar el código.
